{
  "name": "flashcard-ai-mcp",
  "description": "> **By [MEOK AI Labs](https://meok.ai)** \u2014 MEOK AI Labs \u2014 Create study flashcards from text.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "generate_flashcards",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "count": {
            "type": "integer"
          }
        },
        "required": [
          "text"
        ]
      }
    },
    {
      "name": "quiz_me",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "topic": {
            "type": "string"
          },
          "difficulty": {
            "type": "string"
          }
        },
        "required": [
          "topic"
        ]
      }
    },
    {
      "name": "spaced_repetition",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "cards": {
            "type": "string"
          },
          "performance": {
            "type": "string"
          }
        },
        "required": [
          "cards"
        ]
      }
    },
    {
      "name": "export_anki",
      "description": "MEOK AI Labs tool.",
      "parameters": {
        "type": "object",
        "properties": {
          "flashcards": {
            "type": "string"
          }
        },
        "required": [
          "flashcards"
        ]
      }
    }
  ]
}